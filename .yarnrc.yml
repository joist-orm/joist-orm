nodeLinker: node-modules

npmAuthToken: "${NPM_TOKEN-ignore_for_gh_pages_action}"

plugins:
  - path: .yarn/plugins/@yarnpkg/plugin-workspace-tools.cjs
    spec: "@yarnpkg/plugin-workspace-tools"

yarnPath: .yarn/releases/yarn-3.2.0.cjs
