version: "3.1"

services:
  db:
    build:
      context: ./
      dockerfile: db.dockerfile
    ports:
      - 5435:5432
    healthcheck:
      test: pg_isready
      interval: 1s
      timeout: 5s
      retries: 50
