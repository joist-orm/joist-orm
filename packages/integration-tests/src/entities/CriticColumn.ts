import { CriticColumnCodegen, criticColumnConfig as config } from "./entities";
export class CriticColumn extends CriticColumnCodegen {}

// remove once you have actual rules/hooks
config.placeholder();
