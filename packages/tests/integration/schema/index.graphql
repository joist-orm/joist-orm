type Query

type Mutation

scalar DateTime

scalar BigInt

enum FavoriteShape {
  CIRCLE
  SQUARE
  TRIANGLE
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  totalCount: Int!
  nextPage: Int
  currentPage: Int
}
